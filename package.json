{
  "name": "livescan",
  "private": true,
  "version": "1.0.0",
  "description": "Live apps health scanner",
  "scripts": {
    "full-deploy": "sls deploy",
    "deploy:hello": "sls deploy function -f hello",
    "invoke:hello": "sls invoke local -f hello",
    "test": "tslint -p . -t verbose",
    "deploy:getConfig": "sls deploy -f getConfig",
    "invoke:getConfig": "sls invoke local -f getConfig",
    "deploy:main": "sls deploy function -f main",
    "invoke:main": "sls invoke local -f main"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/aws-lambda": "8.10.1",
    "@types/aws-sdk": "^2.7.0",
    "@types/node": "9.6.6",
    "aws-sdk": "^2.226.1",
    "serverless": "1.26.1",
    "serverless-webpack": "4.4.0",
    "ts-loader": "2.3.7",
    "tslint": "5.9.1",
    "typescript": "2.8.3",
    "webpack": "3.11.0"
  }
}
